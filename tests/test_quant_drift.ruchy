// test_quant_drift.ruchy — Quantization drift ordering tests
//
// Verifies that Int8 (higher precision) produces fewer or equal token mismatches
// vs oracle compared to Int4 (lower precision). Specifically:
// Int8 mismatches ≤ Int4 mismatches + 1

import std::fs
import std::process

// --- Helpers (ruchy has no file imports, must be inline) ---

fun run_apr(args) {
    let (stdout, stderr, code) = process::execute("apr", args)?
    if code != 0 { return Err(f"apr failed ({code}): {stderr}") }
    Ok(stdout)
}

fun apr_run_json(model_path, prompt) {
    let stdout = run_apr(["run", model_path, "-p", prompt, "-n", "32", "--json"])?
    Ok(parse_json(stdout))
}

fun load_oracle(slug, prompt_name) {
    let text = fs::read_to_string(f"oracle/{slug}/{prompt_name}.json")?
    Ok(parse_json(text))
}

fun count_mismatches(a, b) {
    let min_len = if a.len() < b.len() { a.len() } else { b.len() }
    let mut m = 0
    for i in range(0, min_len) { if a[i] != b[i] { m = m + 1 } }
    let d = a.len() - b.len()
    if d < 0 { d = -d }
    m + d
}

// --- SmolLM-135M drift ordering ---

@test
fun test_quant_drift_smollm_arithmetic() {
    let oracle = load_oracle("smollm-135m", "arithmetic")?
    let int4 = apr_run_json("models/smollm-135m-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/smollm-135m-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"SmolLM arithmetic: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}

@test
fun test_quant_drift_smollm_completion() {
    let oracle = load_oracle("smollm-135m", "completion")?
    let int4 = apr_run_json("models/smollm-135m-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/smollm-135m-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"SmolLM completion: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}

@test
fun test_quant_drift_smollm_code() {
    let oracle = load_oracle("smollm-135m", "code")?
    let int4 = apr_run_json("models/smollm-135m-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/smollm-135m-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"SmolLM code: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}

@test
fun test_quant_drift_smollm_greeting() {
    let oracle = load_oracle("smollm-135m", "greeting")?
    let int4 = apr_run_json("models/smollm-135m-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/smollm-135m-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"SmolLM greeting: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}

// --- Qwen2-0.5B drift ordering ---

@test
fun test_quant_drift_qwen2_arithmetic() {
    let oracle = load_oracle("qwen2-0.5b", "arithmetic")?
    let int4 = apr_run_json("models/qwen2-0.5b-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/qwen2-0.5b-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"Qwen2 arithmetic: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}

@test
fun test_quant_drift_qwen2_completion() {
    let oracle = load_oracle("qwen2-0.5b", "completion")?
    let int4 = apr_run_json("models/qwen2-0.5b-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/qwen2-0.5b-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"Qwen2 completion: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}

@test
fun test_quant_drift_qwen2_code() {
    let oracle = load_oracle("qwen2-0.5b", "code")?
    let int4 = apr_run_json("models/qwen2-0.5b-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/qwen2-0.5b-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"Qwen2 code: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}

@test
fun test_quant_drift_qwen2_greeting() {
    let oracle = load_oracle("qwen2-0.5b", "greeting")?
    let int4 = apr_run_json("models/qwen2-0.5b-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/qwen2-0.5b-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"Qwen2 greeting: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}

// --- GPT-2 drift ordering ---

@test
fun test_quant_drift_gpt2_arithmetic() {
    let oracle = load_oracle("gpt2-124m", "arithmetic")?
    let int4 = apr_run_json("models/gpt2-124m-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/gpt2-124m-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"GPT-2 arithmetic: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}

@test
fun test_quant_drift_gpt2_completion() {
    let oracle = load_oracle("gpt2-124m", "completion")?
    let int4 = apr_run_json("models/gpt2-124m-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/gpt2-124m-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"GPT-2 completion: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}

@test
fun test_quant_drift_gpt2_code() {
    let oracle = load_oracle("gpt2-124m", "code")?
    let int4 = apr_run_json("models/gpt2-124m-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/gpt2-124m-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"GPT-2 code: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}

@test
fun test_quant_drift_gpt2_greeting() {
    let oracle = load_oracle("gpt2-124m", "greeting")?
    let int4 = apr_run_json("models/gpt2-124m-int4.apr", oracle.prompt)?
    let int8 = apr_run_json("models/gpt2-124m-int8.apr", oracle.prompt)?
    let int4_miss = count_mismatches(int4.tokens, oracle.tokens)
    let int8_miss = count_mismatches(int8.tokens, oracle.tokens)
    assert(int8_miss <= int4_miss + 1, f"GPT-2 greeting: Int8 ({int8_miss}) should be ≤ Int4 ({int4_miss}) + 1")
}
